import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.firebase.yoobic',
  appName: 'yoobic-ionic',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
